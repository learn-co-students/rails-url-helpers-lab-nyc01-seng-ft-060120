<h1><%= @student.to_s %></h1>
<p><%= @student.active ? "This student is currently active." : "This student is currently inactive." %></p>
<p><%= link_to 'Toggle Activate', activate_student_path(@student) %></p>
<h3><%= link_to 'Return to Index Page', students_path %></h3>